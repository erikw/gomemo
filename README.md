# gomemo

A small note taking app written in Go

# Build
Run:

```bash
make build
```


# Development
Run the server with

```bash
go run ./cmd/gomemo
PORT=8080 go run ./cmd/gomemo
```

or with live reload:

```bash
go tool air
```
